var myItems = app.activeDocument.allPageItems;var foundObjects = Array();var rectangleObjects = Array();for (var i = 0; i < myItems.length; i++){    var myItem = myItems[i];    if (myItem instanceof TextFrame) {        $.writeln("TextFrame");    } else {      //  $.writeln("myItem: " + myItem.constructor.name + " parent: " + myItem.parent.constuctor.name);        if(myItem.parent instanceof Character)        {             foundObjects.push(myItems[i]);        }        if(myItem instanceof Rectangle)        {             rectangleObjects.push(myItems[i]);        }    }}for (var i = 0; i < foundObjects.length; i++){    var foundObject = foundObjects[i]; }for (var i = 0; i < rectangleObjects.length; i++){    var rectangleObject = rectangleObjects[i];     $.writeln("rectangleObject name: " + rectangleObject.name);}