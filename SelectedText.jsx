if (app.documents.length != 0){    //If the selection contains more than one item, the selection     //is not text selected with the Type tool.    if (app.selection.length == 1)    {        $.writeln("Single Selection, selected item constructor name: " + app.selection[0].constructor.name);        //Evaluate the selection based on its type.         switch (app.selection[0].constructor.name)        {            case "InsertionPoint":             case "Character":            case "Word":            case "TextStyleRange":            case "Line":            case "Paragraph":            case "TextColumn":             case "Text":            case "Story":            //The object is a text object; pass it on to a function.                 myProcessText(app.selection[0],app.selection[0].constructor.name);                break;            //In addition to checking for the above text objects, we can             //also continue if the selection is a text frame selected with             //the Selection tool or the Direct Selection tool.            case "TextFrame":            //If the selection is a text frame, get a reference to the             //text in the text frame.                 myProcessText(app.selection[0].texts.item(0),app.selection[0].constructor.name);                break;                        default:                alert("Single Selection: " + app.selection[0].constructor.name);                break;         }    } else if (app.selection.length > 1)    {        alert("Selected: " +  app.selection.length + " items, item[0].constructor.name: " + app.selection[0].constructor.name);    } else     {        alert("No Selection!");    }}function myProcessText(text,type){//~     var textId = -1;//~     if (text.id)//~         textId = text.id;//~     var textLabel = text.label;//~     var textName = text.name;//~     var alertStr = "type: " + type + "id: "  + textId + " label: " + textLabel + " name: " +  textName + " contents: " + text.contents;    var alertStr = "type: " + type + " contents: " + text.contents;    alert(alertStr);}    